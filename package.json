{
  "name": "compile-json-stringify",
  "version": "0.1.2",
  "description": "Compile a JSON.stringify() function with type hints for maximum performance",
  "main": "compile-json-stringify.js",
  "files": [
    "compile-json-stringify.js"
  ],
  "engines": {
    "node": ">=6"
  },
  "license": "MIT",
  "author": "Nathan Woltman <nwoltman@outlook.com>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nwoltman/compile-json-stringify.git"
  },
  "homepage": "https://github.com/nwoltman/compile-json-stringify#readme",
  "bugs": {
    "url": "https://github.com/nwoltman/compile-json-stringify/issues"
  },
  "keywords": [
    "compile",
    "json",
    "stringify",
    "fast"
  ],
  "devDependencies": {
    "coveralls": "^3.0.2",
    "eslint": "^5.1.0",
    "mocha": "^5.2.0",
    "nyc": "^12.0.2",
    "should": "^13.2.1",
    "sinon": "^6.1.3"
  },
  "scripts": {
    "lint": "eslint *.js test/*.js",
    "test-only": "nyc --reporter=html --reporter=text-summary mocha",
    "test": "npm run lint && npm run test-only",
    "coveralls": "nyc report --reporter=text-lcov | coveralls"
  }
}
